name: Pi AI Agent

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  pi-agent:
    uses: alex/.profile/.forgejo/workflows/pi.yml@master
    secrets: inherit
